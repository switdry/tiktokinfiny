{
  "name": "tikfinity-clone",
  "version": "3.0.0",
  "description": "Clon de TikFinity - Herramientas completas para TikTok LIVE (Sound Alerts, TTS, Overlays, Goals, Chatbot, Music, Actions)",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc && tsc -p tsconfig.frontend.json",
    "build:frontend": "tsc -p tsconfig.frontend.json",
    "build:backend": "tsc",
    "start": "npm run build && node dist/server.js",
    "dev": "npm run build && nodemon --watch src --exec \"npm run build && node dist/server.js\"",
    "watch": "tsc --watch"
  },
  "keywords": [
    "tiktok",
    "live",
    "comments",
    "tts",
    "text-to-speech",
    "typescript"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "tiktok-live-connector": "^2.1.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
